<% if instance.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(instance.errors.count, "error") %> prevented this <%= instance.class.name.downcase %> from being saved:</h2>

  <ul>
    <% instance.errors.full_messages.each do |m| %>
    <li><%= m %></li>
    <% end %>
  </ul>
</div>
<% end %>
